name: Test
on:
  push:
    branches:
      - 'main'
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World"
      - name: Print goodbye
        run: |
          echo "test1"
          echo "test2"
